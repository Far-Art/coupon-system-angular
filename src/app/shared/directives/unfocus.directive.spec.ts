import { UnfocusDirective } from './unfocus.directive';

describe('UnfocusDirective', () => {
  it('should create an instance', () => {
    const directive = new UnfocusDirective();
    expect(directive).toBeTruthy();
  });
});
