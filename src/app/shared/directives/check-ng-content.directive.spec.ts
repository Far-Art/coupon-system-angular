import { CheckNgContentDirective } from './check-ng-content.directive';

describe('CheckNgContentDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckNgContentDirective();
    expect(directive).toBeTruthy();
  });
});
